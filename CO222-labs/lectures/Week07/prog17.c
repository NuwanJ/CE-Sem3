/* endless loops 


*/


int main(){

	while(1); 	// infinite loop
	for(;;);	// infinite loop with for,
			// the second infinite loop is dead-code as
			// the program will not come out of the
			// first one
	return 0;
}
